export class FormeInfo {
    public baseForme: number;
    public formeNumber: number;
    public formeSpriteOffset: number;

    constructor(baseForme: number, formeNumber: number, formeSpriteOffset: number) {
        this.baseForme = baseForme;
        this.formeNumber = formeNumber;
        this.formeSpriteOffset = formeSpriteOffset;
    }
}
